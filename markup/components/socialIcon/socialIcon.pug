mixin socialIcon(data)
    section.social
      a.social__link(href=data.link)
        != pugHelpers.Icon.call(locals, { iconName: "icon-" + data.icon, className: 'social__icon', iconWidth: 15, iconHeight: 19})
